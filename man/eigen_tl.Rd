% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transfer_learning.R
\name{eigen_tl}
\alias{eigen_tl}
\title{Fold-adjusted covariance-path eigenspace transfer learning}
\usage{
eigen_tl(
  X_target,
  source,
  rank,
  n_source = NULL,
  folds = 5L,
  source_fraction_grid = c(0, 0.05, 0.1, 0.2, 0.35, 0.5, 0.65, 0.8, 0.9, 0.95, 1),
  center = TRUE,
  fold_id = NULL,
  method = c("one_se", "min"),
  standard_error_multiplier = 1,
  eigen_solver = c("full", "rspectra")
)
}
\arguments{
\item{X_target}{Target individual-level data matrix.}

\item{source}{An `ldtl_source_moments` object or a source covariance matrix.}

\item{rank}{Eigenspace size. A number strictly between zero and one is the
cumulative explained-variance threshold computed once from the full target
data. A positive integer is a fixed rank. The resulting integer rank is
shared by every target fold and path candidate.}

\item{n_source}{Source sample size. Required when `source` is a matrix and
checked against the stored sample size when source moments are supplied.}

\item{folds}{Number of target validation folds.}

\item{source_fraction_grid}{Increasing effective-source fractions in
`[0, 1]`, including zero and one.}

\item{center}{If `TRUE`, center each training and validation fold at the
corresponding training-fold target mean.}

\item{fold_id}{Optional fold labels. Supply a shared vector when comparing
sources so all source candidates use identical target folds.}

\item{method}{Path selector. The default paired one-standard-error rule
favors more transfer among competitive candidates; `"min"` selects the
minimum reconstruction-risk candidate.}

\item{standard_error_multiplier}{Nonnegative multiplier for `method =
"one_se"`. The default is one.}

\item{eigen_solver}{Eigensolver used after the integer rank is fixed.
`"full"` uses the existing CppMatrix full eigendecomposition. `"rspectra"`
independently activates RSpectra and computes only the leading `rank`
eigenpairs. A fractional `rank` still requires one full-target
eigendecomposition to determine the common integer cutoff.}
}
\value{
An object of class `eigen_tl` containing the selected full-data
  covariance, eigenspace, complete held-out path scores, paired uncertainty,
  and source-transfer diagnostics.
}
\description{
Fit the source-transfer path proposed by Teacher B. Each grid point is an
effective fraction of the available source sample. Its covariance pooling
weight is recomputed for every target training fold, and the selected path
point is refitted using all target observations.
}
\details{
`method = "one_se"` returns the most transferred candidate whose paired
held-out target-score deficit is within one standard error of the empirical
winner. `method = "min"` returns the candidate with minimum held-out
reconstruction risk, equivalently maximum captured target score.
}
