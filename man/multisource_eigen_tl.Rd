% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multisource_tl.R
\name{multisource_eigen_tl}
\alias{multisource_eigen_tl}
\title{Multi-source covariance-path eigenspace transfer learning}
\usage{
multisource_eigen_tl(
  X_target,
  sources,
  rank,
  folds = 5L,
  transfer_weight_grid = c(0, 0.05, 0.1, 0.2, 0.35, 0.5, 0.65, 0.8, 0.9, 0.95, 1),
  center = TRUE,
  fold_id = NULL,
  method = c("one_se", "min"),
  standard_error_multiplier = 1
)
}
\arguments{
\item{X_target}{Target individual-level numeric matrix with observations in
rows.}

\item{sources}{Named list of at least two `ldtl_source_moments` objects or
source covariance matrices.}

\item{rank}{Eigenspace size. A number strictly between zero and one is the
target cumulative explained-variance threshold, computed separately from
each target training fold and from the full target data. A positive
integer is a fixed rank shared by all fits.}

\item{folds}{Number of target validation folds.}

\item{transfer_weight_grid}{Strictly increasing direct covariance weights in
`[0, 1]`, including zero and one.}

\item{center}{If `TRUE`, center each training and validation fold at the
corresponding training-fold target mean.}

\item{fold_id}{Optional fold labels. Supply a shared vector to reuse the
exact target folds across methods.}

\item{method}{Path selector. The default paired one-standard-error rule
favors more transfer among competitive candidates; `"min"` selects the
minimum reconstruction-risk candidate.}

\item{standard_error_multiplier}{Nonnegative multiplier for `method =
"one_se"`. The default is one.}
}
\value{
An object inheriting from `eigen_tl`. It contains the selected
  covariance and eigenspace, full and fold-specific source compositions,
  complete held-out scores, and the full-data joint CovTL estimator using
  the same source library.
}
\description{
Learn one convex source direction jointly from all supplied source
covariances, then select the amount of transfer along that direction using
held-out target reconstruction scores. In each target training fold, let
`T` be the fold covariance, `D_s = S_s - T`, and
`A_sr = <D_s, D_r>_F`. The source composition is
\deqn{\widehat\pi=\arg\min_{\pi\geq0,\ 1^T\pi=1}\pi^TA\pi.}
For direct covariance weights `w` on the supplied grid, the eigenspace path
is fitted to
\deqn{C(w)=(1-w)T+w\sum_s\widehat\pi_sS_s.}
}
\details{
The source summaries are resolved once and reused in every target fold.
Target covariance and target variance are re-estimated independently in
every training fold. The target variance is retained for the corresponding
joint CovTL fit but is not used to choose the source composition or EigenTL
path weight.

`method = "one_se"` returns the most transferred candidate whose paired
held-out target-score deficit is within one standard error of the empirical
winner. `method = "min"` returns the candidate with minimum held-out
reconstruction risk, equivalently maximum captured target score.
}
