% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transfer_learning.R
\name{path_tpca_max_score}
\alias{path_tpca_max_score}
\title{Max-score fold-adjusted covariance-path transfer PCA}
\usage{
path_tpca_max_score(
  X_target,
  source,
  rank,
  n_source = NULL,
  folds = 5L,
  source_fraction_grid = c(0, 0.05, 0.1, 0.2, 0.35, 0.5, 0.65, 0.8, 0.9, 0.95, 1),
  center = TRUE,
  fold_id = NULL
)
}
\arguments{
\item{X_target}{Target individual-level data matrix.}

\item{source}{An `ld_source_moments` object or a source covariance matrix.}

\item{rank}{Number of leading target eigenvectors.}

\item{n_source}{Source sample size. Required when `source` is a matrix and
checked against the stored sample size when source moments are supplied.}

\item{folds}{Number of target validation folds.}

\item{source_fraction_grid}{Increasing effective-source fractions in
`[0, 1]`, including zero and one.}

\item{center}{If `TRUE`, center each training and validation fold at the
corresponding training-fold target mean. The exact known-mean theory uses
`center = FALSE`.}

\item{fold_id}{Optional integer fold labels. Supplying these is the preferred
way to make comparisons exactly reproducible. Otherwise balanced labels
are randomly permuted using R's current RNG state.}
}
\value{
An object of class `path_tpca_max_score` containing the selected
  full-data covariance/projector, complete fold scores, path weights, and
  fold assignment.
}
\description{
Teacher-B covariance-path estimator using ordinary target cross-validation.
A grid point is an effective fraction of the available source sample, so the
final grid point is exact sample-size pooling in every training fold. The
empirical target-score maximizer is then refitted on all observations.
}
\details{
This is a separate estimator from the analytic CovTL and EigenTL families.
It does not use source fourth moments, although an `ld_source_moments` object
may be supplied as a convenient source covariance/sample-size container.
}
