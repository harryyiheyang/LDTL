% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poet_banding.R
\name{poet_banding}
\alias{poet_banding}
\title{POET with banding and FSPD positive-definite modification}
\usage{
poet_banding(
  S = NULL,
  n = NULL,
  K = NULL,
  alpha = 1,
  eig_min = 0,
  X = NULL,
  scale = FALSE,
  factor_method = c("ACT", "D.ratio"),
  eig = NULL,
  factors = NULL
)
}
\arguments{
\item{S}{Optional symmetric covariance or correlation matrix.}

\item{n}{Sample size used to form `S`. Required when `S` is supplied and
`K` is not supplied.}

\item{K}{Optional bandwidth. Overrides the theoretical default.}

\item{alpha}{Bandable covariance smoothness parameter used in the theoretical
bandwidth. Default is 1.}

\item{eig_min}{Minimum eigenvalue target for FSPD. Default is 0.}

\item{X}{Optional individual-level data matrix. Used to compute `S` when
`S` is not supplied and to infer `n`.}

\item{scale}{If `TRUE`, center and standardize `X` before computing `S`.
Default is `FALSE`, assuming `X` has already been scaled.}

\item{factor_method}{Method used to select the number of POET factors.
`"ACT"` uses adjusted correlation thresholding and is the default;
`"D.ratio"` uses the eigenvalue difference-ratio rule. The lower bound is
5 and the upper bound is the rank reaching 90 percent cumulative
eigenvalue mass.}

\item{eig}{Optional full eigendecomposition of `S`, supplied as a list with
`values` and `vectors`. If `NULL`, it is computed internally.}

\item{factors}{Optional fixed number of POET factors. When supplied, it
overrides `factor_method`.}
}
\value{
A positive-semidefinite POET regularized correlation matrix. Set
  `eig_min` to a positive value to require strict positive definiteness.
}
\description{
Estimate a factor component from the leading eigenpairs of `S`, band the
idiosyncratic covariance, and apply FSPD linear shrinkage to the sparse
idiosyncratic estimator. The selected spike eigenvalues receive the S-POET
positive-part bias correction.
}
