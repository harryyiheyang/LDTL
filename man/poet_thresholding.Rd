% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poet_thresholding.R
\name{poet_thresholding}
\alias{poet_thresholding}
\title{POET with MCP thresholding and FSPD positive-definite modification}
\usage{
poet_thresholding(
  S = NULL,
  n = NULL,
  lambda = NULL,
  eig_min = 0,
  X = NULL,
  scale = FALSE
)
}
\arguments{
\item{S}{Optional symmetric covariance or correlation matrix.}

\item{n}{Sample size used to form `S`. Required when `S` is supplied and
`lambda` is not supplied.}

\item{lambda}{Optional scalar threshold. Overrides the theoretical default.
If missing, `2 * max(sqrt(log(p) / n), 1 / sqrt(p))` is used.}

\item{eig_min}{Minimum eigenvalue target for FSPD. Default is 0.}

\item{X}{Optional individual-level data matrix. Used to compute `S` when
`S` is not supplied and to infer `n`.}

\item{scale}{If `TRUE`, center and standardize `X` before computing `S`.
Default is `FALSE`, assuming `X` has already been scaled.}
}
\value{
A positive-semidefinite POET regularized correlation matrix. Set
  `eig_min` to a positive value to require strict positive definiteness.
}
\description{
Estimate a factor component from the leading eigenpairs of `S`, threshold the
idiosyncratic covariance on correlation scale, and apply FSPD linear
shrinkage to the sparse idiosyncratic estimator.
}
